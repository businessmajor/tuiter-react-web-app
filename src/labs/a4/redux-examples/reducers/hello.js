//const hello = () => ({ message: 'Hello World' });

function Asd() {
    const ewq = [321, 432, 543, 654, 765];
    const tre = (iuy, ytr) => {
      const poi = ewq.map((iuy) => (iuy === ytr ? 999 : iuy));
      console.log(poi);
    };
    return (
      <div>
        <button onClick={() => tre(ewq, 543)}>BUTTON</button>
      </div>
    );
  }
  export default Asd;

//export default hello;